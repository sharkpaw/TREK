services:
  trek:
    build:
      context: .
      dockerfile: Dockerfile
      args:
        APP_VERSION: "3.0.22"
    # Map syntax — Coolify rejects list entries without "=" (e.g. bare SERVICE_URL_TREK_3000).
    environment:
      NODE_ENV: production
      JWT_SECRET: ${SERVICE_BASE64_64_JWTSECRET}
      PORT: "3000"
      # Recommended. Generate with: openssl rand -hex 32.
      ENCRYPTION_KEY: ${SERVICE_BASE64_64_ENCRYPTIONKEY}
      TZ: ${TZ:-Europe/Istanbul}
      LOG_LEVEL: ${LOG_LEVEL:-info}
      ALLOWED_ORIGINS: ${ALLOWED_ORIGINS:-}
      # Public base URL — required for OIDC redirect URI and email notification links.
      APP_URL: ${SERVICE_URL_TREK}
      FORCE_HTTPS: "true"
      TRUST_PROXY: "1"
#      DEFAULT_LANGUAGE: en
#      HSTS_INCLUDE_SUBDOMAINS: "false"
#      COOKIE_SECURE: "false"
#      ALLOW_INTERNAL_NETWORK: "false"
#      - ALLOW_INTERNAL_NETWORK=false # Set to true if Immich or other services are hosted on your local network (RFC-1918 IPs). Loopback and link-local addresses remain blocked regardless.
#      - OIDC_ISSUER=https://auth.example.com # OpenID Connect provider URL
#      - OIDC_CLIENT_ID=trek # OpenID Connect client ID
#      - OIDC_CLIENT_SECRET=supersecret # OpenID Connect client secret
#      - OIDC_DISPLAY_NAME=SSO # Label shown on the SSO login button
#      - OIDC_ONLY=false # Set true to force SSO-only mode: disables password login and registration, overrides Admin > Settings toggles, cannot be changed at runtime
#      - OIDC_ADMIN_CLAIM=groups # OIDC claim used to identify admin users
#      - OIDC_ADMIN_VALUE=app-trek-admins # Value of the OIDC claim that grants admin role
#      - OIDC_SCOPE=openid email profile # Fully overrides the default. Add extra scopes as needed (e.g. add groups if using OIDC_ADMIN_CLAIM)
#      - OIDC_DISCOVERY_URL= # Override the OIDC discovery endpoint for providers with non-standard paths (e.g. Authentik)
#      - ADMIN_EMAIL=admin@trek.local # Initial admin e-mail — only used on first boot when no users exist
#      - ADMIN_PASSWORD=changeme      # Initial admin password — only used on first boot when no users exist
#      - MCP_RATE_LIMIT=300 # Max MCP API requests per user per minute (default: 300)
#      - MCP_MAX_SESSION_PER_USER=20 # Max concurrent MCP sessions per user (default: 20)
    volumes:
      - './data:/app/data'
      - './uploads:/app/uploads'
    restart: unless-stopped
    healthcheck:
      test:
        - CMD
        - wget
        - '-qO-'
        - 'http://127.0.0.1:3000/api/health'
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s
    security_opt:
      - 'no-new-privileges:true'
    cap_drop:
      - ALL
    cap_add:
      - CHOWN
      - SETUID
      - SETGID
    tmpfs:
      - '/tmp:noexec,nosuid,size=64m'
    # Traefik birden fazla Docker ağında IP görürse yanlış ağa gidebilir; Coolify bunu sık yapar.
    labels:
      - traefik.docker.network=coolify
    networks:
      - coolify

networks:
  coolify:
    external: true